let hello = "Hello, World!";
println(hello);  // Expected output: "Hello, World!"

hello = 'Hello, World!';

println(hello);


let myString = "hello, world";
println(myString[0]);  // Expected output: 'h'
println(myString[7]);  // Expected output: 'w'


let myString = "hello, world";
println(myString[-5]);  // Expected output: 'w' (if negative indexing is supported)